name: CI Workflow Lint Reusable

on:
 workflow_call:

jobs:
 workflow_lint:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
     with:
      fetch-depth: 0

   - name: Lint GitHub workflows
     uses: reviewdog/action-actionlint@v1
     with:
      fail_level: error
